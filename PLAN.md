# План: проявлення, XMP, Dodge & Burn, дрібні інструменти

Це план, а не обіцянка. Кожен пункт має розмір, залежності й межу, за яку
він свідомо не йде. Роадмапа лишається в `spec.md` §11 — тут те, ЯК.

---

## 0. Принцип, з якого все випливає

**Ми не переписуємо Camera Raw. Ми поважаємо її рішення.**

`spec.md` §4 уже дав аргумент проти власного демозаїка: місяці роботи
заради гіршого результату. Той самий аргумент стосується всієї ACR —
кольорові профілі під кожну камеру, профілі об'єктивів, відновлення
світлів, локальні корекції. Це інша програма, і в тебе вона вже є.

Але XMP міняє постановку. Замість «зробити як ACR» — **«прочитати, що
фотограф вирішив в ACR, і застосувати те, що можемо, чесно сказавши про
решту»**. Це не заміна проявнику, це міст до нього.

---

## 1. Що з XMP ми можемо, а що ні

Ділиться на три яруси, і плутати їх не можна.

### Ярус A — точно, без наближень

| параметр XMP | що робимо |
|---|---|
| `crs:HasCrop`, `CropTop/Left/Bottom/Right` | кроп, точно |
| `crs:CropAngle` | поворот, точно |
| `tiff:Orientation` | орієнтація, точно |
| `crs:WhiteBalance="As Shot"` | камерний ББ у rawpy, точно |

Геометрія — це арифметика, тут наближень немає. **Уже це вартує роботи:**
якщо кадр кроплений в ACR, конвеєр зараз про це не знає і рахує маску та
радіус по повному кадру.

### Ярус B — наближено, з чесним підписом

| параметр | наше наближення | чому не точно |
|---|---|---|
| `Exposure2012` | `exp_shift = 2^EV` | в ACR це не чистий підсил, а тон-мап зі згортанням світлів |
| `Temperature`, `Tint` | перерахунок у `user_wb` | потрібна матриця камери; вийде близько, не тотожно |
| `ToneCurvePV2012` | LUT по точках | інтерполяція в ACR своя; форма збігається, значення — приблизно |
| `Saturation`, `Vibrance` | HSV-масштаб | vibrance в ACR нелінійний і щадить тілесні тони |
| `Sharpness`, `LuminanceSmoothing` | параметри rawpy | зовсім інші алгоритми |

Ці застосовуємо, але в звіті пишемо `≈`, і в UI поруч стоїть «наближено».

### Ярус C — читаємо, показуємо, НЕ вдаємо

`Clarity2012`, `Dehaze`, `Texture`, профілі об'єктивів, `Upright`,
локальні корекції (`MaskGroupBasedCorrections`), калібрування камери.

Це власні алгоритми Adobe. Підробити їх «схожим ефектом» — гірше, ніж не
робити нічого: результат буде іншим, а виглядатиме як застосований.
Тому: прочитати, вивести списком «не застосовано», і все.

> **Наслідок для §6.2, який не можна проґавити.** Поріг детекції
> міряється поверх проявлення. Якщо кадр проявлено нашим ярусом B, а
> пороги калібровані на TIFF з ACR — вони не переносяться. Або калібруємо
> заново на своєму проявленні, або лишаємо ACR-TIFF еталоном, а XMP —
> зручністю. Це рішення, а не деталь.

---

## 2. Порядок робіт і чому саме такий

```
  A. проявлення (rawpy-параметри)  ──┐
  B. XMP: ярус A (геометрія)       ──┼──> C. калібрування на 20-30 кадрах
  B'. XMP: ярус B (тон)            ──┘         │
                                                ├──> D. Dodge & Burn
                                                └──> E. дрібні інструменти
```

**Калібрування посередині, а не спочатку.** Ти обрав «спочатку
калібрування», і це правильний інстинкт — але калібрувати можна лише
поверх стабільного входу. Зараз вхід нестабільний у двох місцях: декодер
(rawpy vs ImageIO дає 1478 проти 4613 плям) і відсутність кропа з ACR.
Спершу зафіксувати вхід, потім міряти.

D і E від калібрування не залежать: Dodge & Burn працює з низькою
частотою, а не з порогом детекції.

---

## 3. Етапи

### A. Проявлення RAW — ~1 день

Після звірки з конвеєром RawTherapee (spec.md §16) стало видно, що
прогалина менша, ніж здавалось: libraw закриває етапи 1-9, і бракує рівно
трьох речей — **кроп, тон-крива, RGB-криві**.

Новий `retouch/develop.py`: дата-клас `DevelopParams` -> `rawpy.Params`
для того, що вже вміє libraw (експозиція, ББ, гама, насиченість, вибір
демозаїка, шумозаглушення, відновлення світлів), плюс власні кроп і
тон-крива після декодування. Повзунки в UI, прев'ю на проксі.

Межа: жодного власного тон-мапу і жодного retinex/dehaze. Тільки те, що
вміє libraw, плюс три пункти прогалини.

### B. XMP, ярус A — ~1 день

`retouch/xmp.py` на `xml.etree` (stdlib, нової залежності немає).
Читає сайдкар `IMG.xmp` або вбудований блок. Застосовує кроп, поворот,
орієнтацію. Решту параметрів складає у звіт.

Перевірка: кадр, кроплений в ACR, має дати ту саму рамку.
**Потрібен файл, що пройшов через ACR** — у нас такого немає, тестувати
доведеться на твоєму.

### B'. XMP, ярус B — ~2-3 дні

Експозиція, ББ, крива, насиченість. Кожен параметр окремо, з приміткою
«наближено». Тон-крива — найцінніше і найточніше з усього яруса.

### C. Калібрування — не про код

20-30 своїх кадрів через `scripts/selftest.py --sweep` і `--dry-run`,
з ФІКСОВАНИМ проявленням. Результат — нові дефолти в дата-класах і
перезаписана таблиця §6.2.

### D. Dodge & Burn — ~2 дні

`retouch/dodgeburn.py`. Guided filter по яскравості низької частоти,
різниця віддається шаром 50% сірого в Soft Light.

Це **справжній шар корекції** в сенсі §1 — на відміну від пластики,
Photoshop складе його назад точно. Інваріант: `soft_light(base, gray50)
== base` там, де корекції немає.

Обмеження маскою шкіри — та сама механіка, що в лікуванні.

### E. Дрібні інструменти — по ~0.5 дня кожен

Усі класичні, усі спираються на класи, які вже є:

| інструмент | клас | що робить |
|---|---|---|
| судини в білку | `l_eye`, `r_eye` | знижує насиченість червоного в склері |
| відбілювання зубів | `mouth` | знижує жовтизну, піднімає яскравість |
| матування | `skin` | стискає верхні відсотки яскравості (відблиск) |
| тон шкіри | `skin` | вирівнює хроматичність у низькій частоті |

Кожен — окремий шар з альфою, кожен вимикається.

---

## 4. Чого цей план свідомо не робить

- не відтворює Clarity, Dehaze, Texture і локальні корекції ACR;
- не читає профілі об'єктивів і не робить корекцію дисторсії;
- не обіцяє збіг з ACR піксель у піксель — і не має обіцяти;
- не береться за Reblum-подібну наскрізну модель: це `spec.md` §11 v1.0,
  і воно потребує датасету з твоїх PSD, а не ще одного алгоритму.

---

## 5. Найбільший ризик

Що ми напишемо ярус B, він даватиме «майже як ACR», і на цьому
заспокоїмось. «Майже» в кольорі означає, що кадри з нашого проявлення й
кадри з ACR не можна класти в одну серію, а пороги, зняті на одних, не
дійсні для інших.

Тому в звіті кожного прогону має бути видно, ЧИМ проявлено і які
параметри XMP проігноровано. Мовчазне «наближено» — гірше за відсутнє.

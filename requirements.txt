numpy>=1.24,<3

# Стеля 4.11 — не забаганка. Колеса opencv для Intel macOS з 4.11 збираються
# під macosx_13_0, а робоча машина — Monterey 12 (spec.md §2). Без стелі pip
# бачить лише несумісні колеса, мовчки падає на збірку зі сирців і годинами
# крутить cmake на двох ядрах. Остання версія з колесом під 12.0 — 4.10.0.84.
# Ставити так: pip install --only-binary=:all: -r requirements.txt
#   — тоді замість годинної збірки одразу видно, що колеса немає.
opencv-python-headless>=4.8,<4.11

PyYAML>=6.0

# Опційні. Обидва не потрібні, щоб конвеєр працював, тому їх тут немає
# списком — CLAUDE.md забороняє додавати обов'язкові залежності.
#
# для ONNX-моделей (face-parsing, LaMa):
#   pip install onnxruntime
#
# для RAW: без нього RAW читається через macOS ImageIO (нуль залежностей),
# але Apple підмішує в високу частоту своє шумозаглушення й різкість —
# на тому самому порозі виходить утричі більше «дефектів» (spec.md §4).
#   pip install rawpy
# або одразу:  pip install 'retouch-lab[raw]'

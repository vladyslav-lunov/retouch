# Ручні перевірки

Числа в колонці «має вийти» зняті на MacBook Pro A1502 (2 ядра, 8 ГБ,
macOS 12) з `models/resnet18.onnx`, `models/yunet.onnx`, `models/lama.onnx`.
Твої можуть відрізнятися на десятки відсотків за часом, але не в рази за
кількістю плям чи покриттям маски — якщо відрізняються, щось не так.

Усе нижче — з кореня репозиторію, з активованим середовищем на Python 3.9.

```bash
python3 -c "import cv2, numpy, rawpy, onnxruntime; print('стек на місці')"
```

---

## 0. Тести — спершу вони

```bash
python3 tests/run_all.py
```

Тринадцять наборів, 95 тестів, близько хвилини. Якщо не «усе зелене» —
далі йти немає сенсу.

---

## 1. Макет: чи конвеєр узагалі живий

```bash
python3 scripts/make_fixture.py -o fixtures
python3 -m retouch.cli fixtures/PORTRAIT.tif -o out --preview
```

| має вийти | |
|---|---|
| маска | ~21%, джерело heuristic |
| радіус | ~4.1 px |
| плям | ~34 |
| дотиків | ~0.14% |

Відкрий `out/PORTRAIT_preview.png`. Верхній ряд — загальний план,
нижній — кропи 1:1. **Судити про ретуш можна лише за нижнім рядом.**

---

## 2. Табель на макеті: наскільки добре, а не «чи впало»

```bash
python3 scripts/selftest.py
python3 scripts/selftest.py --sweep
```

Має показати ~40% влучань детектора і 0 px лікування поза маскою.
Сорок відсотків — це не помилка складання, це чесна оцінка евристики
на макеті (spec.md §6.2).

---

## 3. Свій кадр, повний, з моделями

RAW читається напряму — конвертувати нічого не треба.

```bash
python3 -m retouch.cli ~/Downloads/IMG_0059.CR3 -o out --preview \
  --face-model models/resnet18.onnx --face-detector models/yunet.onnx
```

| має вийти | |
|---|---|
| read | `raw=rawpy`, якщо rawpy стоїть |
| маска | 3-6% повного кадру, джерело `face-parsing+yunet` |
| плям | 100-200 |
| дотиків | ~0.1% |
| разом | 40-60 с на 26 Мп |

**Без `--face-detector` буде гірше і мовчки**: face-parsing стискає весь
кадр до 512 px, обличчя стає завширшки ~100 px і модель ламається
(§15.1). Ознака: маска 1-2% і клас `hat` там, де капелюха немає.

Перевірити ваги, якщо береш інші:

```bash
python3 scripts/check_face_model.py models/resnet18.onnx ~/Downloads/IMG_0059.CR3
```

Контракт скрипт перевірить сам, **порядок класів — тільки оком** на
кольоровій карті: шкіра має лягти на шкіру, волосся на волосся.

---

## 4. Керування класами: прикраси на шкірі

Це головна причина, чому набір класів — параметр. Ланцюжок лежить у класі
`neck`, і модель має рацію: це шия, це шкіра. Але ретушувати його не треба.

```bash
python3 -m retouch.webui
```

Вкладка **Маска** -> зняти галочку з `neck` -> «Застосувати» -> «Перегнати».

| | маска | плям | дотиків на грудях |
|---|---|---|---|
| `neck` увімкнено | ~16% | ~143 | ~5% площі кропа |
| `neck` вимкнено | ~9% | ~105 | **0** |

Дивитися на вкладці **1:1**, клацнувши по ланцюжку. З увімкненим `neck`
видно, як ланки розмиває і рветься петля підвіски.

---

## 5. Видалення об'єкта

```bash
python3 -m retouch.webui
```

Вкладка **Видалення** -> замалювати -> «Видалити». Модель вибирається у
списку «інпейнт» зліва: UI сам знаходить її в `models/`. Якщо там стоїть
«Telea (без моделі)» — LaMa не знайдено, перевір, чи файл на місці.

Ознака, що LaMa справді працює: в етапах видно `inpaint lama`, а не
`inpaint telea`, і попередження про глибину не з'являється.

Без `--lama-model` працює запасний Telea, і на глибині понад 24 px він
дає пляму, а не фон — конвеєр про це скаже сам. З LaMa:

```bash
python3 -m retouch.cli IMG.tif -o out --remove-mask mask.png \
  --lama-model models/lama.onnx
```

**Ціна: 12-14 с на кожен прохід 512×512.** Ланцюжок на грудях (87k px,
глибина 23 px) — близько 50 с. Людина в повний зріст піде на хвилини.

---

## 6. Пластика

Вкладка **Пластика**. Тягнути / роздути / стягнути / закрутити, потім
«Застосувати до кадру».

Порядок етапів має бути саме такий: `warp -> skin-mask -> freq-split ->
detect -> heal`. Маска перераховується ПІСЛЯ деформації — інакше вона
описувала б стару геометрію і з'їхала б відносно кадру.

Перевірити, що працює як задумано:
- «сила поля» на 0 має повернути кадр рівно як був;
- у теці виводу поруч із шарами лежить `*_warp__s8_lim*.png` — це поле
  зміщення, його можна застосувати ще раз або послабити;
- `_00_base.tif` після пластики — **деформований** кадр, не вхідний файл.
  Так і має бути (§14.2), інакше шар не зійшовся б із базою.

---

## 7. Шари справді складаються

Головна обіцянка проєкту: накладання шару на базу дає рівно зведений файл.

```bash
python3 - <<'PY'
import cv2, numpy as np, glob, os
d = "out"; stem = sorted(glob.glob(f"{d}/*_00_base.tif"))[0].split("_00_")[0]
rd = lambda p: cv2.imread(p, cv2.IMREAD_UNCHANGED).astype(np.float32) / 65535
cur = rd(f"{stem}_00_base.tif"); flat = rd(f"{stem}_99_flat.tif")
for L in sorted(glob.glob(f"{stem}_0[1-9]_*.png")):
    lay = rd(L); a = lay[:, :, 3]
    cur = cur * (1 - a[..., None]) + lay[:, :, :3] * a[..., None]
    print("наклав", os.path.basename(L))
print(f"похибка: {np.abs(cur - flat).max() * 65535:.1f} кванта 16 біт")
PY
```

Має бути кілька квантів. Десятки — щось зламано.

---

## 8. Бюджет пам'яті

```bash
python3 scripts/bench.py --mp 24
```

Пік процесу має бути в межах 2 ГБ. Якщо пішло за 4 — десь знову з'явився
буфер розміру кадру, і на 8 ГБ конвеєр перестане запускатися (§2).

---

## 9. Пакетна обробка

```bash
python3 -m retouch.cli SHOOT/ -o out --batch --preset shoot.yaml
```

| має статись | |
|---|---|
| битий файл | ЗБІЙ у рядку, решта йде далі |
| `IMG_002.yaml` поруч із `IMG_002.tif` | підхопиться, у рядку буде `[IMG_002.yaml]` |
| повторний запуск | усе `проп. вже оброблено` |
| `--no-resume` | переробить усе заново |

Наприкінці — підсумок: скільки зроблено, пропущено, збоїв, і по кожному
збою причина.

Покадровий пресет накладається ПОВЕРХ загального і не протікає на
наступний кадр. Перевірити просто: постав у сайдкар інший `threshold` і
подивись у `--dry-run`, що сусідні кадри його не взяли.

Паралелізму немає навмисно: ядер два, а кадр на 26 Мп бере до 2 ГБ, тож
два процеси підуть у своп і стануть повільнішими за один (§2).

---

## 10. Dodge & Burn

    python3 -m retouch.cli IMG.tif -o out --dodge-burn --db-strength 0.6 \
      --face-model models/resnet18.onnx --face-detector models/yunet.onnx

| має вийти | |
|---|---|
| етап | `[dodge-burn] сила 0.6, торкнулися ~15% кадру` |
| файл | `*_03_dodgeburn_softlight.png` — 16-біт сірий |
| поза маскою | нуль пікселів корекції |

**Режим накладання стоїть у назві файлу.** Це не звичайний шар з альфою:
в Normal він дасть сіру пляму на пів кадру. Правильно — Soft Light.
Сценарій 7 вище перевіряє складання; для D&B до нього треба додати
`soft_light` з `retouch.dodgeburn`.

Сила 1.0 вирівнює тон повністю і виглядає пластиково — це не помилка, а
те, що просили. Робоче значення десь 0.4-0.7.

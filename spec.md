# retouch-lab — специфікація

Локальний інструмент ретуші портретів: автоматика шкіри та видалення
об'єктів. Для власного використання, не продукт.

---

## 1. Мета і головний принцип

Компроміс між пакетною обробкою та ручним контролем. Не «зроби красиво»,
а **«зроби чорнову роботу і покажи, що саме ти зробив»**.

Тому головне архітектурне рішення:

> Конвеєр ніколи не віддає готовий піксель як єдиний результат.
> Він віддає **шари корекції з альфою** плюс усі проміжні маски.

Це дає рівно те, чого немає в Retouch4me і Reblum: видно, що система
вважала шкірою, що вважала дефектом і що змінила. Не подобається —
викручується opacity, домальовується маска, вимикається окремий шар.

Другий принцип: **лікуємо тільки високу частоту**. Тон і об'єм у низькій
частоті не чіпаються взагалі. Саме через це не з'являється «замилена
пляма» — світлотінь лишається оригінальна, а текстура підміняється
текстурою справжньої шкіри поруч, а не розмиттям.

## 1.1 Куди це виросло

Написане вище лишається чинним як ПРИНЦИПИ. Але мета проєкту з часу
першої редакції змінилась, і не зафіксувати це — значить лишити
специфікацію, яка вводить в оману.

Було: інструмент, що робить чорнову роботу і віддає шари в Photoshop.
Стало: **самодостатній застосунок для обробки фотографій**, у якому
Photoshop не бере участі взагалі (його на робочій машині і немає).

Цільова форма — вкладки за етапами конвеєра проявника (§16):

| вкладка | що робить | етапи §16 |
|---|---|---|
| **Проявлення** | експозиція, ББ, крива, кроп — те, що робить RawTherapee | 1-15 |
| **Ретуш** | шкіра, видалення об'єктів, D&B, дрібні інструменти | 16 |
| **Пластика** | геометрія, поле зміщення | окремо (§14) |

Станом на цю редакцію форма досягнута: вкладки покривають усі етапи, що
є в коді, і будуються зі схеми дата-класів — див. §18.

Плюс дві речі, яких у першій редакції не було зовсім:

- **пресети** — набір параметрів, збережений і переносний між кадрами;
- **пакетна обробка** — пресет на теку, без нагляду.

### 1.2 Навіщо насправді потрібен XMP

Не щоб копіювати Camera Raw. XMP тут — **формат, яким агент віддає
результат аналізу**.

Задум такий: агент дивиться на кадри, розуміє, що з ними не так, і пише
пресети. Не один універсальний, а кілька варіантів під конкретну зйомку.
Людина обирає з них, а не крутить двадцять повзунків з нуля.

З цього випливають вимоги, яких немає у звичайного формату налаштувань:

1. **машинописний**. Схема має бути настільки явною, щоб агент писав
   валідний пресет без здогадок. Наші дата-класи вже є такою схемою —
   бракує лише її вивантаження в машиночитний вигляд;
2. **семантичний**. `exposure: +0.35` агент обґрунтує, `param_7: 0.35` —
   ні. Назви параметрів мають означати те, що означають у фотографії;
3. **частковий**. Пресет має задавати ЛИШЕ те, що змінює, а не весь стан.
   Інакше два пресети неможливо накласти один на одного;
4. **із поясненням**. Пресет від агента має нести причину: «підняв тіні,
   бо гістограма впирається в нуль». Без цього обрати з десяти
   неможливо — вони виглядатимуть однаково.

Пункт 4 — те, чого немає ні в XMP, ні в .pp3, ні в жодному форматі
проявника. Вони зберігають ЩО, але не ЧОМУ. Для роботи з агентом це
головне поле, а не примітка.

## 2. Залізо і його наслідки

| | MacBook Pro A1502 | MSI Katana 15 HX |
|---|---|---|
| CPU | 2 ядра Intel | i7-14650HX |
| GPU | Iris 5100/6100 | RTX 5070 (sm_120, torch cu128) |
| RAM | 8 ГБ | 32 ГБ |
| ОС | стеля Big Sur / Monterey | — |

Наслідки, які визначили дизайн:

- **Стеля Xcode** на Big Sur — 12.5.1. Тому Python, а не Swift. Питання
  нативного порту відкладене, не закрите (див. §11).
- **ONNX Runtime на CPU**, без CUDA. LaMa 512×512 — одиниці секунд.
  Прийнятно для «натиснув і чекаєш», неприйнятно для інтерактиву.
  Тому інтерактивного канвасу в v1 немає.
- **8 ГБ RAM проти 50 Мп файлу.** 50 Мп × 3 канали × float32 = 600 МБ
  на один буфер. Конвеєр тримає одночасно `img`, `low`, `high`, `high2`,
  `result` — це вже 3 ГБ. Тому: обробка по одному файлу, кроп-режим для
  інпейнту, і жодних зайвих копій масивів (див. §9).
- **Тренування — тільки на Katana.** Mac інферить готові ONNX.

## 3. Модулі

```
retouch/
  imageio.py    I/O, 16-біт, збереження розрядності
  rawread.py    RAW: rawpy (libraw) і ImageIO через ctypes
  freqsep.py    частотне розкладання, вибір радіуса
  masks.py      маска шкіри: евристика + BiSeNet ONNX
  blemish.py    ЯДРО — детекція дефектів і лікування по HF
  inpaint.py    LaMa ONNX, кроп-і-вшивка
  layers.py     збірка шарів корекції
  warp.py       пластика: поле зміщення (§14)
  preview.py    оглядовий аркуш: загальний план + кропи 1:1
  pipeline.py   оркестрація, Session, звіт по етапах, debug-дамп
  cli.py        точка входу
  webui.py      локальний UI на stdlib http.server
  static/       сторінка UI
tests/
  synth.py          синтетична «шкіра» + макет портрета (make_face)
  test_blemish.py   інваріанти ядра
  test_inpaint.py   інваріанти видалення
  test_warp.py      інваріанти пластики
  test_pipeline.py  наскрізні: від файлу до записаних шарів
scripts/
  fetch_models.py   завантаження ваг
  assemble.jsx      складання шарів у Photoshop
  make_fixture.py   макет портрета у 16-бітний TIFF
  crop_face.py      кроп голови (каскад Хаара) — тимчасовий, до face-parsing
  selftest.py       табель на макеті: повнота, хибні спрацювання
  bench.py          бюджет часу й пам'яті (§9)
```

`cli.py` і `webui.py` — два фронтенди до одного `pipeline.Session`.
Послідовність етапів не дублюється: UI не переганяє конвеєр сам, він
драйвить той самий сеанс.

Залежність одна: `pipeline` знає про всіх, решта не знає одне про одного,
крім `blemish -> freqsep`. Це навмисно — кожен модуль тестується окремо.

## 4. Формат даних

- Усередині все — **`float32` BGR у `[0..1]`**. BGR, бо OpenCV, і не
  варто конвертувати туди-сюди на кожному кроці.
- Розрядність вхідного файлу запам'ятовується і повертається на запис.
  16-бітний TIFF з Camera Raw не має деградувати до 8 біт.
- **Працюємо в кодуванні вхідного файлу, з гамою, не в лінійному.**
  Це свідоме рішення: у лінійному просторі частотне розкладання дає
  інший вигляд, ніж звичне в Photoshop, і високочастотний шар перестає
  поводитися як «текстура». Для ретуші важлива відповідність звичці,
  а не фізична коректність.
- **RAW читаємо, але демозаїк — чужий.** Початково RAW не читався взагалі;
  «колись знадобиться» настало. Позиція не змінилась у головному: власний
  демозаїк ми НЕ пишемо, це місяці роботи заради гіршого результату. Беремо
  готовий, і саме ті два, які тут і були названі:

  | | ставиться | якість для нас |
  |---|---|---|
  | **rawpy** (libraw 0.21+) | `pip install rawpy` | керована й відтворювана |
  | **ImageIO** (Core Image) | нічого, є в macOS | рендер Apple, некерований |

  Перевага в rawpy, і не з естетичних міркувань. Apple підмішує в RAW своє
  шумозаглушення й різкість, а вони живуть саме у високій частоті — тобто
  в шарі, з яким працює весь конвеєр. Заміряно на одному кадрі, однаковий
  поріг, однакова ділянка щоки:

  | джерело | HF std | маска шкіри | знайдено плям |
  |---|---|---|---|
  | rawpy | 0.0064 | 69% | 1478 |
  | ImageIO | 0.0145 | 94% | 4613 |
  | sips → TIFF | — | 91% | 4558 |

  ImageIO і sips збігаються — це один рушій, ctypes лише прибирає з дороги
  зовнішню команду. Але **декодер — така сама змінна калібрування, як
  проявник**: таблиця §6.2 дійсна лише для фіксованого декодера. Тому
  конвеєр завжди друкує, чим саме прочитано (`raw=rawpy`), і має
  `--raw-decoder` для примусового вибору.

  ImageIO викликається через `ctypes` напряму, без PyObjC і без зовнішніх
  команд: сторонньої залежності не з'являється, тимчасові файли не
  пишуться. EXIF-орієнтацію застосовуємо самі — `CGImageSourceCreateImageAtIndex`
  віддає кадр як знято, і без повороту `radius_for` міряв би ширину
  обличчя по висоті кадру.

- 16-бітний TIFF з Camera Raw лишається **основним** входом: ACR — це
  проявник, під який калібрувалися пороги, і жоден із двох декодерів його
  не замінює.

Але відмова на RAW має бути ВНЯТНОЮ. OpenCV на `.CR3` просто віддає
`None`, і користувач бачив «не читається» на файлі, який лежить на місці
й відкривається будь-де ще. Тепер RAW упізнається за розширенням, і
конвеєр каже, що це рішення, а не поломка, — плюс дає однорядковий обхід
для швидкої перевірки:

```
sips -s format tiff FILE.CR3 --out FILE.tif
```

macOS Image I/O читає CR3 і віддає 16-бітний RGB TIFF за секунди
(перевірено на 26 Мп). Для ПЕРЕВІРКИ конвеєра цього досить. Для
калібрування (§6.2) — ні: sips рендерить RAW своїм тон-мапом, не як ACR,
а від тон-мапу залежить, як саме дефект розкладається між частотами.

## 5. Маска шкіри

Два шляхи з однаковим інтерфейсом (`build_skin_mask`), перемикання —
наявністю файлу моделі:

1. **Евристика** (`heuristic_skin_mask`): YCrCb-діапазон + морфологія +
   ерозія від краю. Без моделей, працює одразу.

   > **Заміряно на реальному кадрі — і це не «груба, але робоча».**
   > Вуличний портрет 26 Мп: бежевий камінь аркади, бежеве пальто,
   > шкіра. Усе в одному діапазоні YCrCb. Маска віддала **91% кадру**.
   > Далі посипалось усе: `radius_for` побачив «обличчя» завширшки з
   > кадр і дав радіус **20.8 px замість правильних 4.7**, детектор
   > знайшов **4558 «дефектів»** — по каменю, тканині й волоссю, — а
   > лікування порізало волосини на шматки й затерло ланцюжок на шиї.
   > На кропі самої голови не краще: 93%.
   >
   > Причина структурна, і підкрутка порогів її не лікує: це тест на
   > КОЛІР, а не на обличчя. Він працює рівно тоді, коли фон не збігається
   > зі шкірою за кольором, — тобто в студії на рівному тлі. На вулиці
   > не працює взагалі.
   >
   > **Кроп голови не рятує — перевірено на чотирьох масштабах.** Маска
   > лишається 90-95% і на «голова+плечі», і на щільному кропі самого
   > обличчя. Причина та сама: русяве волосся за кольором теж «шкіра».
   > На такому кропі покриття 90% навіть чесне — у кадрі справді майже
   > все шкіра, — але результат однаково непридатний: лікування йде по
   > волоссю, бровах, віях і контурі губ, торкається 15.8% кадру.
   >
   > Отже, обхідного шляху немає. До face-parsing інструмент придатний
   > лише там, де маска ВИБІРКОВА: темне волосся, фон не кольору шкіри.
   >
   > Тому конвеєр тепер **зупиняється**, якщо маска накрила більше
   > `max_skin_fraction` (0.6) кадру, і каже, що робити. Мовчки віддати
   > шар, який ріже волосся, — це проти §1. Обійти: `--force-mask`.
2. **BiSeNet face-parsing**, CelebAMask-HQ, 19 класів, через ONNX.
   Дає окремо шкіру, брови, очі, губи, волосся, шию.

   **Перевірено на реальних вагах** (yakhyo/face-parsing, ResNet18/34,
   готовий ONNX). Контракт збігається з тим, що припускав код: 512×512,
   ImageNet-нормалізація, BGR→RGB, BCHW, argmax по каналах. Порядок
   класів звірено з `utils/prepare_labels.py` того ж репозиторію і
   підтверджено оком на кольоровій карті — таблиця `CELEBA_CLASSES`
   правильна, переписувати нічого.

   Той самий кроп голови, той самий поріг:

   | маска | час | покриття | радіус | плям | дотиків |
   |---|---|---|---|---|---|
   | евристика | 0.2 с | 91.8% | 10.4 px | 1933 | 11.53% |
   | **resnet18** | **0.9 с** | **16.5%** | **3.7 px** | **151** | **0.68%** |
   | resnet34 | 1.2 с | 17.9% | 3.7 px | 156 | 0.74% |

   151 пляма — рівно та величина, яку §9 і закладав («50-150»). resnet34
   дає практично те саме за втричі більший файл, тож дефолт — resnet18.

   > **BiSeNet треба годувати КРОПОМ ГОЛОВИ, а не повним кадром.** Модель
   > навчена на вирівняних портретах 512×512, і `build_skin_mask` зараз
   > просто стискає весь кадр до 512. На кропі це працює, на повному
   > кадрі 26 Мп обличчя стає завширшки ~100 px і модель ламається:
   > заміряно 1.4% шкіри і 28% «капелюха» там, де капелюха немає.
   > Поки що кроп робиться руками (`scripts/crop_face.py`). Правильне
   > рішення — детекція обличчя всередині `build_skin_mask`, кроп,
   > парсинг, і повернення маски в координати кадру.

   Коли маска стала правдивою, проступили дві хиби детектора, яких доти
   просто не було видно за загальним провалом:
   - **прикраси на шкірі**. Ланцюжок на грудях лежить усередині класу
     `neck`, тобто законно вважається шкірою, і детектор бачить його як
     низку темних плям. 33% усіх дотиків припадає на нижню третину
     кадру, де він висить;
   - **вії** на межі ока і повіки.

   Це вже §13, питання 1, у загальнішому вигляді: `max_elongation` їх не
   ловить, бо і ланцюжок, і вія розпадаються на короткі сегменти з майже
   квадратним bbox.

Зони виключення (`EXCLUDE_CLASSES`) розширюються дилатацією і
віднімаються від шкіри. Уся маска ерозується на `erode` px, щоб не
лікувати контур обличчя — там донор завжди буде з фону.

Маска обрізає не лише детекцію, а й **альфу лікування**, і це не одне
й те саме. Після детекції альфу ще розширюють `dilate(margin)` і
`blur(feather)` — разом 5-8 px, і на повіці цього досить, щоб дотик
заповз на око: заміряно 11 px за маску і ~1700 px усередині очей при
нульовій детекції там. Ерозія з цього абзацу не рятувала, бо захищає
попередній етап. Обріз різкий, а не розмитий: розмитий лишав би хвіст
за маскою, тобто те саме заповзання, тільки слабше. Шва не дає — маска
вже ерозована всередину, тож обріз падає на шкіру.

> **Перевірити перед довірою:** порядок класів у `CELEBA_CLASSES`
> залежить від конкретних ваг. Різні перезаливки BiSeNet трапляються
> з переставленими індексами. Для цього є `scripts/check_face_model.py`:
> він показує контракт входів-виходів, частку кадру по кожному класу і
> кладе поруч кольорову карту — оригінал, класи, накладення. Контракт
> скрипт перевіряє сам, а от **порядок класів підтверджує лише око**:
> шкіра має лягти на шкіру, волосся на волосся, губи на губи.
>
> Обгортку `FaceParser` перевірено на макеті моделі потрібної форми
> (1x3x512x512 -> 1x19x512x512): код працює, `build_skin_mask`
> перемикається на модель. Це знімає питання про обгортку, але не про
> ваги.

## 6. Автоматика шкіри

### 6.1 Детекція

Багатомасштабний смуговий банк по яскравості HF-шару. Для кожної сигми
з `scales` згладжуємо HF і беремо пікселі, де відгук нижчий за
`-threshold` (темні дефекти). Об'єднання по масштабах, зв'язні
компоненти, фільтри:

- площа в `[min_area, max_area]` — відсікає шум і великі тіні;
- витягнутість bbox < `max_elongation` — відсікає волосини та краї;
- усе всередині маски шкіри.

Результат сортується за спаданням контрасту. Це важливо: `--limit N`
тоді означає «прибери N найпомітніших, решту зроблю руками».

### 6.2 Калібрування порога

Заміряно на синтетиці, 15 відомих дефектів (`tests/synth.py`, seed=3):

| threshold | компонент | влучань | зайвих |
|---|---|---|---|
| 0.008 | 352 | 15/15 | ~337 |
| **0.012** | **21** | **15/15** | **~6** |
| 0.018 | 15 | 12/15 | ~3 |
| 0.025 | 2 | 2/15 | ~0 |

`0.012` — обраний дефолт. Хибні спрацювання тут дешеві: лікування
підміняє текстуру на текстуру, і на чистій ділянці зміна візуально
нульова. Пропуск дорожчий за хибне спрацювання.

**Це синтетика.** Перекалібрувати на 20-30 своїх кадрів до того, як
довіряти числу. **Зроблено — і результат перевернув постановку питання;
див. §6.2.1.**

#### Переміряно після зміни дефолтного набору класів

`MaskParams.skin_classes` більше не містить `neck` (§15). CLAUDE.md
вимагає переміряти цю таблицю після зміни дефолту, тож: **обидві таблиці
вище не змінилися жодною цифрою**, і це не збіг. Вони зняті на
синтетиці й на макеті, де маска будується евристикою по YCrCb, а не
face-parsing'ом — класів там немає взагалі, і `skin_classes` на них не
впливає.

Змінюється лише замір на РЕАЛЬНОМУ кадрі, і ось він
(`scripts/calibrate.py`, 26 Мп, обличчя 742 px, face-parsing+YuNet):

| поріг | плям (neck у наборі) | підозрілих | плям (дефолт, без neck) | підозрілих |
|---|---|---|---|---|
| 0.008 | 382 | 24% | 284 | **0%** |
| 0.010 | 212 | 23% | 160 | **0%** |
| **0.012** | **154** | **25%** | **113** | **0%** |
| 0.014 | 105 | 32% | 70 | **0%** |
| 0.018 | 64 | 44% | 36 | **0%** |
| 0.025 | 50 | 64% | 18 | **0%** |

«Підозрілі» — частка знахідок у класах, де дефект шкіри малоймовірний
(шия, одяг, вухо, волосся, фон). Істини на реальному кадрі немає, тож це
нижня оцінка браку, а не точна — але вона рахується без розмітки.

**Головне в цій таблиці — не рівень, а НАПРЯМОК.** З `neck` у наборі
частка браку РОСТЕ разом із порогом: 24% на 0.008 і 64% на 0.025.
Інтуїція «підняти поріг, щоб менше хапало зайвого» тут працює навпаки,
і причина проста: ланки ланцюжка контрастніші за будь-який справжній
дефект шкіри, тому поріг першими відсіює саме справжні. Порогом цю
проблему не лікують взагалі.

Без `neck` частка нульова на всіх порогах, тобто дві осі незалежні, і
класи вирішуються ПЕРШИМИ. Це та сама вимога, що в §15, тепер із числом.

Ланцюжок на кропі 1:1 після цієї зміни збігається з оригіналом побітово:
0 пікселів зі 460×460, максимальна різниця 0 з 255.

Сам поріг **0.012 лишається недоторканим**: 20-30 реальних кадрів для
його калібрування ще не міряно, а робити висновок з одного кадру — це
рівно та помилка, від якої застерігає §6.2. Скрипт готовий і чекає теки.

Наскільки синтетика бреше, видно, щойно з'являється обличчя. Той самий
замір на макеті-портреті (`scripts/selftest.py --sweep`, обличчя 900 px,
20 дефектів, з маскою шкіри та світлотінню):

| threshold | компонент | влучань | зайвих |
|---|---|---|---|
| 0.008 | 80 | 14/20 | 66 |
| **0.012** | **27** | **8/20** | **19** |
| 0.018 | 14 | 1/20 | 13 |
| 0.025 | 0 | 0/20 | 0 |

Тобто дефолт, який на плоскому клапті давав 15/15 при шести зайвих, на
обличчі дає 8/20 при дев'ятнадцяти. Причини структурні, і всі три
відсутні на клапті: світлотінь зганяє частину енергії дефекту в низьку
частоту; `radius_for` занижує радіус на ~17% (§6.3); контури ока, брови
й губи дають сильний край одразу за маскою — з 27 спрацювань 13 лежать
біля зон виключення.

Числа з цієї таблиці — теж НЕ калібрування: контраст плям у макеті
заданий рукою. Вона годиться рівно на одне — порівнювати дві версії
коду між собою. Але висновок «таблиця на клапті завищує якість»
структурний, і на реальні кадри переноситься.

### 6.3 Радіус частотки

Виводиться з **ширини обличчя**, а не з мегапікселів кадру. Це не
дрібниця: поясний портрет і кроп голови з того самого файлу мають
однакові мегапікселі, але зовсім різний масштаб пор. Прив'язка до
ширини маски шкіри стабільна і на кропі, і на іншій камері.

Калібрування: обличчя шириною 1200 px → радіус 6 px.

**Відоме розходження, не виправлене.** Ширина береться як горизонтальний
розмах маски шкіри, а це не ширина обличчя: волосся перекриває щоки з
боків, і маска ще й ерозована на `erode` px. На макеті-портреті
(`tests/synth.py::make_face`) розмах виходить стабільно ~82% від
справжньої ширини: 480→391, 620→509, 980→814. Радіус відповідно
занижений десь на 18%, і дефекти з'їжджають із високої частоти в низьку,
де детектор їх не бачить — на тому ж макеті 0/20 влучань при обличчі
480 px проти 11/20 при 980 px.

Лишено як є свідомо. Будь-який інший оцінювач — найширший рядок маски,
площа через еліпс, компенсація ерозії — підганявся б під макет, а це
рівно та помилка, від якої застерігає §6.2. Міряти треба на реальних
кадрах, разом із порогом.

### 6.2.1 Калібрування на 44 реальних кадрах: єдиного порога немає

Прогнано `scripts/calibrate.py` на 44 кадрах однієї людини (CR3, rawpy,
face-parsing, класи за замовчуванням). Питання ставилось як «яке число
поставити дефолтом». Відповідь виявилась іншою: **такого числа не існує.**

З фіксованим порогом `0.012` частка ТОРКНУТОЇ ШКІРИ по кадрах:

| зона | кадрів | що це означає |
|---|---|---|
| < 1% | 10 | не зроблено майже нічого |
| 1-5% | **15** | робоча зона |
| 5-10% | 5 | забагато |
| > 10% | **14** | згладжування — те, чого проєкт не робить (§1) |

Медіана 3.97%, максимум **23.9%**. Тобто дефолт працює на 15 кадрах із
44, а на 14 активно шкодить. Розкид не 20% і не вдвічі — від 0% до 24%.

Дивитись на кроп 1:1 обов'язково, і саме він пояснює числа. На кадрі
IMG_0800 (716 плям, 21% шкіри) чоло й ніс виходять восковими: детектор
ловить ПОРИ, лікування підміняє їх текстурою донора, і сумарно це
згладжування, тільки досягнуте іншим способом. Заборона з §12 стосується
розмиття, але результат тут той самий, і формально чисте лікування його
не виправдовує.

**Що переноситься між кадрами — не поріг, а ЦІЛЬ.** Контраст 0.012
означає різне на різних кадрах; «торкнути не більше 3% шкіри» означає
одне й те саме скрізь, бо це відповідь на питання «скільки шкіри ми
переписали». Тому додано `DetectParams.target_coverage`: поріг
підбирається під кадр драбиною (`Session.solve_threshold`), ціль лишається
спільною на зйомку.

Те саме калібрування, ціль 3%:

| | <1% | 1-5% | 5-10% | >10% | медіана | макс |
|---|---|---|---|---|---|---|
| поріг 0.012 | 10 | 15 | 5 | **14** | 3.97% | 23.9% |
| ціль 3% | 5 | **35** | 3 | **1** | 2.42% | 14.0% |

У робочій зоні 35 кадрів замість 15; у згладжуванні один замість
чотирнадцяти. Підібрані пороги при цьому лягли по ВСІЙ драбині — 10
кадрів просять 0.008, 10 просять 0.025. Це й є доказ, що одного числа
немає.

**Чому ціль не ввімкнена за замовчуванням.** Якби вона була, пресет, що
задає `detect.threshold`, мовчки б ігнорувався: підбір перезаписує поріг,
а відрізнити «задано явно» від «дефолт дата-класу» в дата-класі нічим.
Мовчки з'їдений параметр гірший за неоптимальний дефолт (§1). Тому ціль
вмикається явно — `--target-coverage 0.03` або `detect.target_coverage`
у пресеті, — і це рекомендований спосіб роботи з різнорідною зйомкою.

**Ціна:** 4-30 с на кадр 26 Мп. Драбина обривається на першому ж порозі,
що вкладається в ціль, тож чисті кадри коштують одну пробу, а дорого
платять саме ті, заради яких це й робиться.

**Чого це НЕ дає.** Ціль — бюджет площі, а не гарантія якості. Вона не
відрізняє «торкнули три помітні плями» від «торкнули тридцять дрібниць
тієї ж сумарної площі». Повноти на реальному кадрі не порахувати взагалі:
істини немає, розмітки немає, і §6.2 залишається без колонки «влучань».

### 6.4 Лікування

Для кожної плями шукаємо донора — вікно того самого розміру з
**найчистішою текстурою** поблизу. Реалізація через `boxFilter`:

```
energy   = boxFilter(hf², win)       середня енергія HF у вікні
occupied = boxFilter(інші_плями, win) заборона на вікна з дефектами
valid    = boxFilter(маска_шкіри, win) заборона на вікна поза шкірою
cost     = energy + BIG·(occupied ∨ ¬valid)
```

Мінімум `cost` у радіусі `search_radius` — це і є донор. Копіюємо його
HF у пляму з м'якою альфою з форми плями. Якщо чистого донора поруч
немає — **пропускаємо пляму**, не лікуємо абияк.

Карта вартості рахується **тільки в межах пошуку**, а не на весь кадр.
Спокуса зробити навпаки — порахувати повнокадрову карту й закешувати її
за розміром вікна — розбивається об арифметику: розмірів вікна на
портреті десятки, кожна повнокадрова карта на 24 Мп важить 96 МБ, і кеш
виростає до 5 ГБ там, де всього 8 (§2). Локально відповідь та сама —
перевірено, до біта, — але коштує сотні кілобайт. Заміряно на 6 Мп:
1710 МБ → 74 МБ і 1.7× швидше. Стежить тест (§6.5, пункт 6).

### 6.5 Інваріанти (перевіряються тестами)

1. детектор знаходить ≥80% відомих дефектів;
2. |HF| у центрі плями падає щонайменше втричі;
3. низька частота не змінюється (макс. дельта < 0.02);
4. поза `coverage` кадр ідентичний до біта (після пластики — лише там,
   куди деформація не дотяглася, див. §14);
5. шар корекції реконструює результат: `base·(1−a) + layer·a == result`;
6. лікування не тримає буферів розміру кадру;
7. лікування не виходить за маску шкіри — ні на піксель.

Пункт 5 критичний: якщо він ламається, у Photoshop вийде не те, що
показав конвеєр. Пункт 6 — не про красу, а про те, чи конвеєр взагалі
запуститься на 8 ГБ (§2). Пункт 7 перевіряється на макеті-портреті, бо
на плоскому клапті шкіри маска покриває весь кадр і виходити нема куди.

Заради пункту 5 альфа накопичується композитно, `1−(1−a₁)(1−a₂)`, а не
через `max`. Плями сусідять постійно, і на перекритті їхніх альф `max`
занижує сумарну альфу — тоді `extract_layer`, ділячи на занижене `a`,
мусить робити колір шару екстремальнішим, щоб зійшлася арифметика, і
на світлій шкірі виштовхує його за `[0..1]`, де його зрізає `clip`.
На заміряних кадрах це давало ~5·10⁻⁵ похибки, тобто інваріант не
ламало. Але це запас міцності на випадковість, а не на конструкцію:
з композитною альфою колір шару — опукла комбінація реальних кольорів,
і вилетіти за межі не може взагалі.

## 7. Видалення об'єктів

Вхід — біла маска: намальована пензлем у `retouch-ui` (вкладка
«Видалення») або в Photoshop і збережена PNG. Модель — LaMa через ONNX.

Головна проблема — роздільність: LaMa тренували приблизно на 512 px і
вище неї вона мазюкає. Рішення — **не годувати їй увесь кадр**:

1. кожна зв'язна область маски обробляється окремим кропом з контекстом
   `context` px навколо;
2. якщо кроп ≤ `max_size` — обробка в **рідній роздільності**;
3. якщо більший — зменшуємо, обробляємо, повертаємо, і беремо з
   результату **тільки низьку частоту**, а високу — з оригіналу.
   Так шов не видно і різкість не падає.
4. вшивка з розмитою альфою.

**Висока частота оригіналу дійсна лише поза маскою.** Пункт 3 бере LF від
моделі, а HF з оригіналу — і це вірно рівно доти, доки оригінал у цьому
місці є ФОНОМ. Усередині великої маски оригінал — це те, що видаляють, і
його текстура поверталася в дірку привидом видаленого об'єкта. На дрібному
не видно (там і масштаб 1.0, гілка не працює), на людині заміряно: розкид
у дірці 0.116 замість 0.010. Тому вагу HF гасимо всередині маски. У дірці
лишається чиста низька частота від моделі — м'яко, зате це справді те, що
модель придумала, без домальованого привида.

**Глибина, а не габарит.** Придатність запасного Telea міряємо
`distanceTransform` — найбільшою відстанню від точки маски до її краю.
Волосина через пів кадру має здоровенний bbox і ширину 7 px, і Telea з нею
впорається; кругла пляма того самого габариту — ні. Глибше ніж
`TELEA_USABLE_DEPTH` (24 px) конвеєр каже, що потрібні ваги, а не мовчки
віддає розмазане (§1).

Кожен наступний кроп береться з ПОТОЧНОГО стану кадру, а не з оригіналу.
Інакше друга область, чий контекст `context` px перекриває першу,
вписувала б у кадр прямокутник, зібраний з оригіналу, і мовчки
скасовувала першу правку — при `coverage`, яка все одно рапортує
одиницю. Заразом модель бачить уже виправлений контекст.

Запасний варіант без моделі — `cv2.inpaint` (Telea). Годиться лише для
дрібного, але дозволяє ганяти конвеєр до того, як ваги завантажені.

> **Перевірити:** контракт входів/виходів ONNX-файлу. Припущення в коді —
> `image 1x3xHxW [0..1] RGB` + `mask 1x1xHxW {0,1}`, вихід `[0..255]`.
> Імена входів беруться з сесії, але порядок і нормалізація — здогадка.

## 8. Вихідні файли

```
out/
  PORTRAIT_00_base.tif      оригінал, вихідна розрядність
  PORTRAIT_01_skin.png      16-біт RGBA — шар лікування шкіри
  PORTRAIT_02_remove.png    16-біт RGBA — шар видалення об'єктів
  PORTRAIT_mask_skin.png    що система вважала шкірою
  PORTRAIT_99_flat.tif      зведений результат
  PORTRAIT_debug/           з --debug: low, high, детекція, coverage, diff×4
```

`_debug/06_diff_x4.png` — різниця, підсилена вчетверо. Найшвидший спосіб
зрозуміти, чи система полізла кудись не туди.

## 9. Продуктивність

Бюджет на A1502, портрет 24 Мп:

| етап | очікування |
|---|---|
| читання 16-біт TIFF | 2-4 с |
| face-parsing ONNX 512² | 1-2 с |
| частотне розкладання | 1-2 с |
| детекція (3 масштаби) | 2-4 с |
| лікування 50-150 плям | 3-8 с |
| LaMa, кроп 1024² | 5-15 с |
| запис шарів | 5-10 с |

Разом ~20-45 с на кадр. Для пакетної ночі це нормально.

Якщо впирається в RAM: `low` після `freq_merge` більше не потрібен,
`high` після `heal_blemishes` теж. Явний `del` плюс обробка строго по
одному файлу. Не переходити на тайли, поки не доведено, що треба.

## 10. Моделі та ліцензії

| що | звідки | ліцензія |
|---|---|---|
| BiSeNet face-parsing | yakhyo/face-parsing, готовий ONNX | код MIT, **ваги — від CelebAMask-HQ** |
| onnxruntime | pip, **стеля <1.20** на Monterey | MIT |
| LaMa | `big-lama`, ONNX-експорт | код Apache-2, **ваги CC BY-NC-SA** |
| OpenCV | pip | Apache-2 |

**Версія onnxruntime — теж обмеження заліза.** Колеса для Intel macOS з
1.20 збираються під macosx_13_0, а §2 фіксує стелю Monterey. Остання
придатна — 1.19.2. Для Python 3.13 сумісних колес немає ЖОДНОГО, тож
модельна гілка живе на Python 3.9. Перевірено: інференс іде,
CoreMLExecutionProvider доступний.

**Ваги face-parsing — теж некомерційні, попри MIT на коді.** Код
yakhyo/face-parsing під MIT, але ваги навчені на CelebAMask-HQ, а її
угода — «non-commercial research purposes only», і поширюється вона на
«any portion of derived data». Тобто ваги в тому ж кошику, що й LaMa:
для себе можна, продавати не можна.

**Ваги LaMa — некомерційні.** Для власного використання це нормально.
Якщо колись з'явиться думка продавати — модель доведеться замінити або
навчати свою.

## 11. Дорожня карта

**v0.1 — те, що є.** Маска шкіри, детекція, лікування по HF, шари,
CLI, тести на синтетиці.

**v0.2 — face-parsing, і лише ПОТІМ калібрування.** Порядок саме такий,
і він змінився після першого ж прогону на реальному кадрі (§5).
Спочатку здавалося, що BiSeNet — це покращення поверх робочої евристики.
Виявилось навпаки: без нього інструмент на вуличному портреті не просто
гірший, а шкідливий. І калібрувати threshold немає на чому — поріг
міряється поверх маски, а маска бреше на 91% кадру.

Отже: (1) підключити face-parsing ONNX і звірити порядок класів;
(2) полагодити `radius_for` — з чесною маскою обличчя його нарешті буде
чим міряти (§6.3); (3) аж тоді перекалібрувати threshold, radius,
search_radius на 20-30 власних портретах. Третій крок — не про код.

**v0.3 — LaMa.** Реальні ваги, перевірка контракту, кроп-режим на 50 Мп.

**v0.4 — PSD замість набору PNG.** `pytoshop` пише багатошаровий PSD
напряму. Поки що — `scripts/assemble.jsx`.

**v0.5 — Dodge & Burn. ЗРОБЛЕНО.** Guided filter по LF-яскравості,
різниця віддається шаром 50% сірого в Soft Light. Повністю класично.

Три речі, які з'ясувались під час роботи:

`cv2.ximgproc.guidedFilter` у headless-збірці немає — він у contrib.
Реалізовано через boxFilter: п'ятнадцять рядків і та сама формула.
Гаусів тут не годиться, бо розмиває ЧЕРЕЗ край обличчя, і біля контуру
D&B починає освітлювати фон.

Розмиття краю маски розповзається В ОБИДВА боки, і корекція виходила за
маску на 112 тисяч пікселів. Тепер: ерозія на `feather`, розмиття з
сигмою `feather/3`, і множення на вихідну маску як гарантія. Заміряно
нуль за маскою при будь-якому `feather`.

D&B іде ПІСЛЯ лікування: свіжа пляма — це локальна нерівність тону, і
якби D&B ішов першим, він витрачав би силу на те, що лікування за
секунду прибере зовсім.

Шар пишеться окремим файлом `_03_dodgeburn_softlight.png`, режим у назві:
це НЕ звичайний шар з альфою. Перевірено на записаних файлах — база,
шар шкіри (Normal), D&B (Soft Light) сходяться зі зведеним за 2.5 кванта.

**v1.0 — власна модель.** Ось де Katana має сенс. U-Net, що передбачає
**шар корекції** (різницю до/після), а не готове фото. Датасет —
власні PSD зі зйомок: пара «зведений оригінал / зведений результат»
дає готовий таргет. Тренувати на 5070, експортувати в ONNX, інферити
на Mac. Це не раніше, ніж класичний конвеєр буде відкалібрований —
інакше нема з чим порівнювати.

**Пластика — зроблена, див. §14.** Була відкладена з формулюванням
«окреме поле зміщення + інтерактивний канвас — це інша програма».
Поле зміщення справді окреме, а от «інша програма» не знадобилась:
канвас забороняв §2 через ONNX на CPU, а деформація не потребує моделі
взагалі. Заміряно: проксі 900 px деформується за 26 мс, повний кадр
26 Мп — за 0.86 с.

**Відкладено:** UXP-плагін до Photoshop, нативний Swift-порт.

## 12. Чого проєкт свідомо НЕ робить

- не пише власний демозаїк — RAW проявляє libraw або ImageIO, не ми;
- не має інтерактивного канвасу — у сенсі живого перерахунку на кожен рух
  миші. Локальний UI (`retouch-ui`) з'явився, і він уміє малювати маску
  та вимикати окремі плями, але модель лишилась «натиснув і чекаєш»:
  пензель править МАСКУ, конвеєр переганяється окремою кнопкою. Заборона
  в §2 була суто продуктивнісною (ONNX на CPU), і цієї межі UI не
  переходить. Повний кадр у браузер не їде ніколи: загальний план
  зменшений, кропи 1:1 сервер ріже на запит (26 Мп -> 186 КБ за 30 мс);
- ~~не робить пластику~~ — робить, див. §14. Межа лишилась там, де її
  ставив §2: перерахунку на кожен рух миші немає, деформація застосовується
  до повного кадру окремою дією;
- не згладжує шкіру розмиттям — ніде, ніколи;
- не приймає рішень мовчки: усе, що змінилося, лежить окремим шаром.

## 13. Відкриті питання

1. Хибні спрацювання на волоссі й бровах — чи вистачить `exclude_dilate`,
   чи треба окремий детектор витягнутих структур.
2. Радимо `search_radius=90` при обличчі 1200 px. Чи має він теж
   масштабуватися від ширини обличчя? Ймовірно так.
3. Донор обирається за мінімумом енергії HF. Це дає найгладкішу шкіру,
   що іноді *занадто* гладко. Варіант: обирати не мінімум, а медіану
   серед допустимих — перевірити на реальних кадрах.
4. Чи потрібен окремий режим для великих зморшок (площа > `max_area`),
   чи вони принципово ручна робота.


## 14. Пластика

Деформація тіла й обличчя. Зроблена після того, як заміри показали, що
причина відкладення (§11) до неї не стосується: канвас забороняв §2
через ONNX на CPU, а тут моделі немає взагалі.

### 14.1 Носій — поле зміщення, а не шар

Геометрію не можна віддати шаром із альфою: Photoshop не складе назад те,
що ми пересунули. Тому носієм є **поле зміщення** — воно маленьке, його
видно, його можна зберегти, застосувати до іншої версії того самого кадру
і послабити множенням на число. Множник і є аналогом непрозорості шару, і
саме він тримає §1 там, де шар неможливий.

Поле лежить у зменшеному масштабі (типово 1/8). Деформація за визначенням
гладка — різкий злам у полі дав би розрив на картинці, а не результат, —
тож на 26 Мп це 3.1 МБ замість 300, і жодної деталі не втрачається.

Домовленість про знак: у полі лежить, **звідки** брати піксель, тобто
`result(x) = source(x + D(x))`. Мазок «тягну вправо» кладе від'ємне
зміщення по x. Переплутати тут — отримати дзеркальний результат, тому
знак перевіряється тестом.

Пишеться 16-бітним PNG; крок кванта, масштаб і розмір кадру — у назві
файлу, інакше його неможливо прочитати назад, не вгадуючи.

### 14.2 Місце в конвеєрі: перед шкірою

Спершу форма, потім текстура — як і робить ретушер руками. Інакше ретуш
робилася б по пікселях, які деформація потім пересемплить, а шар корекції
з'їхав би відносно бази.

Наслідок, який треба знати: **пластика — перший етап, який пересемплює
кадр**. Інваріант 4 («поза `coverage` кадр ідентичний до біта») після неї
тримається лише там, куди деформація не дотяглася. Це не недогляд, а
природа геометрії: зберегти піксель можна тільки там, де його не рухали.
Нульове поле — точна тотожність, це перевіряється окремо.

З цього ж випливає, що `_00_base.tif` після пластики — це **деформований**
кадр, а не вхідний файл. Інакше `base·(1−a) + layer·a` не зійшлося б:
лікування рахувалося вже по новій геометрії. Оригінал нікуди не дівається
— він у вхідному файлі, а як саме його зігнуто, записано полем.

### 14.3 Інтерактивність

UI показує зменшену копію (900 px) і деформує саме її: 26 мс проти 0.86 с
на повному кадрі. Копія робиться з ОРИГІНАЛУ, а не з поточного стану —
інакше кожен мазок лягав би на вже деформовану копію й спотворення
накопичувалося б удвічі. Повний кадр перераховується окремою кнопкою,
тобто модель лишилась «натиснув і чекаєш» (§2).

Інструменти: тягнути, роздути, стягнути, закрутити, заморозити. Мазки
накопичуються в одне поле, тож порядок їх застосування не важить і
жоден із них не є «шаром» окремо.


## 15. Керування маскою: модель пропонує, фотограф вирішує

Коли face-parsing запрацював, стало видно, що «шкіра» — це не властивість
пікселя, а РІШЕННЯ під конкретний кадр. Ланцюжок на грудях лежить у класі
`neck`, тобто модель має рацію: це шия, це шкіра. Але ретушувати його не
треба. Жодна інша модель цього не виправить, бо помилки тут немає.

Тому набір класів, які вважаються шкірою, — параметр (`MaskParams.skin_classes`),
а не константа. Заміряно на кроп голови:

| що вважаємо шкірою | маска | плям | дотиків на грудях |
|---|---|---|---|
| skin + nose + neck (дефолт) | 16.5% | 151 | 32.9% |
| skin + nose | 9.2% | 110 | **0.0%** |

Тобто проблема ланцюжка знімається галочкою, а не новою моделлю.

**Перевірено на повному кадрі 26 Мп** (ручний прогін, не синтетика).
Дефолтний набір: маска 3.3%, 154 плями, з них **39 (25%) у класі `neck`**,
усі 32 нижче підборіддя — це ланцюжок. На кропі 1:1 видно, що лікування
рве його на незв'язані шматки, а місцями стирає зовсім. Без `neck`: маска
1.8%, 113 плям, у класі `neck` нуль, ланцюжок побітово збігається з
оригіналом.

Це найдорожчий з відомих збоїв конвеєра: він не падає, не попереджає і
дає файл, який виглядає обробленим. Тому додано **розподіл знахідок по
класах** — він друкується в консолі, лежить у стані UI, іде в досьє
агентові, і при частці ≥15% у підозрілому класі супроводжується
попередженням. «154 плями» і «154 плями, з них 39 на шиї» — це два різні
повідомлення, і лише друге дозволяє помітити підміну.

Другий бік того самого: вибір класів має ПЕРЕЖИВАТИ прогін. До цієї
редакції `/api/classes` міняв лише поточний Config, а «Перегнати» будував
його з форми наново — набір мовчки повертався до дефолтного, і виходила
розбіжність: маска в пам'яті вже без `neck`, а конфіг каже, що він там є.
Тепер вибір кладеться в ПРЕСЕТ, що заразом і правильно по суті: «що
вважати шкірою» — рішення фотографа під кадр, а пресет саме для таких
рішень і є.

Карта класів кешується в `Session.cls`, і `remask()` перебирає набір за
~120 мс без запуску моделі — карта не залежить від того, що ми потім
назвемо шкірою. У UI це живі чекбокси з часткою кадру біля кожного класу.

Разом виходить три рівні контролю, від грубого до тонкого:
1. **модель** дає класи;
2. **фотограф** каже, які з них шкіра;
3. **пензель** править залишок вручну (правки лягають поверх і переживають
   зміну параметрів).

### 15.1 Детекція обличчя: YuNet

`FaceParser` стискав увесь кадр до 512×512, і на 26 Мп обличчя ставало
завширшки ~100 px — модель ламалась (1.4% шкіри, 28% «капелюха»). Тепер
кадр спершу кропиться по обличчю через YuNet (`cv2.FaceDetectorYN`, 227 КБ,
MIT, лежить у самому OpenCV — нової залежності немає).

Повний кадр 26 Мп без ручного кропа: маска 3.3%, радіус 3.6 px, 154 плями,
0.125% дотиків, разом ~44 с. Це вкладається в §9.

Поза рамкою обличчя лишається `background`: що не потрапило в кроп, шкірою
не вважається в будь-якому разі. Групові портрети — окрема розмова: зараз
береться лише найбільше обличчя.

### 15.2 LaMa: розмір диктує модель

Готовий експорт (Carve/LaMa-ONNX) має **намертво зашиті 512×512**, і
доповнення до кратного восьми з §7 його не влаштовує. Тепер розмір
береться з самої моделі: якщо він фіксований — кроп тягнеться до нього і
повертається назад, якщо динамічний — працює старе доповнення.

Контракт ПЕРЕВІРЕНО на справжніх вагах: входи `image` 1×3×512×512 і `mask`
1×1×512×512, вихід у діапазоні 0..255 — рівно те, що припускав §7. Модель
змінює пікселі й ПОЗА маскою (вона генерує кадр цілком), тому вшивка через
альфу в `inpaint_region` не факультативна, а обов'язкова.

**Ціна: 12-14 с на один прохід 512×512** на цій машині. §9 закладав 5-15 с
на кроп 1024², тобто бюджет уже вибрано найменшим можливим тайлом. Видалення
людини, якому потрібно кілька тайлів, коштуватиме хвилини. Це наступне, з
чим доведеться щось робити.


## 16. Орієнтир: де ми на конвеєрі проявника

RawTherapee як КОД нам не годиться: GPL-3, C++, і поточні збірки під macOS
вимагають 26-ї версії проти нашої 12 (§2). Але як **карта місцевості** він
безцінний — це двадцять років чужого досвіду про те, в якому ПОРЯДКУ треба
робити операції з кадром. Порядок тут не деталь: та сама операція до і
після переходу в робочий простір дає різний результат.

Їхній конвеєр проти нашого стану:

| етап RawTherapee | у нас |
|---|---|
| 1. preprocess (чорна точка, CA, гарячі пікселі) | rawpy |
| 2. demosaic | rawpy, 12 алгоритмів |
| 3. retinex | не робимо |
| 4. highlight recovery | rawpy |
| 5. white balance | rawpy |
| 6. **spot removal** | **`blemish.py` — це вже ми** |
| 7. crop | **прогалина** |
| 8. -> робочий простір | rawpy |
| 9. noise reduction | rawpy |
| 10-11. dehaze / DRC | ярус C (не вдаємо) |
| 12. **local adjustments** | **маски й пензель у UI — це ми** |
| 13-14. tone curve | **прогалина** |
| 15. process RGB (канали, криві) | прогалина, частково |
| 16. **process Lab** (локальний контраст, різкість, вейвлети, soft light) | **тут живе ВЕСЬ retouch-lab** |
| 17. -> вихідний простір | `imageio.py` |

**Головний висновок: retouch-lab — це розгорнутий етап 16.** Частотне
розкладання, лікування, майбутній Dodge & Burn, локальний контраст — усе
це в RawTherapee лежить в одному кроці «process Lab», і саме там йому
місце. Ми не будуємо проявник, ми будуємо те, що йде ПІСЛЯ нього.

Звідси два практичні наслідки.

**Прогалина мала.** Між тим, що віддає libraw, і придатною базою для
ретуші бракує рівно трьох речей: кроп, тон-крива, RGB-криві. Не «написати
Camera Raw», а три пункти. Це переписує §1 плану (`PLAN.md`) з «базове
проявлення» на конкретний список.

**Рішення §4 підтверджується.** RawTherapee тримає лінійний RGB угорі
конвеєра і переходить у перцептивний простір (Lab, CIECAM02) унизу — рівно
там, де різкість, локальний контраст і вейвлети. Наш вибір працювати в
кодуванні з гамою, а не в лінійному, — це той самий перехід, і зроблений
він у тому ж місці конвеєра. §4 обґрунтовував це звичкою до Photoshop;
виявляється, воно збігається і з тим, як влаштований серйозний проявник.

### 16.1 Наслідок для §1: кому призначені шари

§1 будувався навколо передачі в Photoshop: конвеєр віддає шари, а збирає
їх ретушер у чужій програмі. Якщо мета — **самодостатній інструмент**, ця
опора зникає, і змінюється не принцип, а адресат.

Принцип лишається: нічого не робиться мовчки, кожен дотик — окремий шар,
який можна послабити або вимкнути. Але збирає їх тепер власний UI, а не
Photoshop. З цього випливає:

- шари потрібні UI не менше, а більше: він єдиний, хто їх покаже;
- `scripts/assemble.jsx` перетворюється на сумісність, а не на робочий шлях;
- §11 v0.4 (PSD через pytoshop) з'їжджає з «головного способу віддати
  результат» на «формат обміну з тими, у кого Photoshop є»;
- натомість піднімається пріоритет того, чого досі не було: керування
  непрозорістю шарів у самому UI.


## 17. Дрібні інструменти

Чотири класичні корекції, жодної моделі. Вони з'явились не тому, що
знайшовся алгоритм — алгоритми тут на десять рядків, — а тому, що
з'явилась КАРТА КЛАСІВ. Face-parsing каже, де око, де рот, де шкіра, і
після цього кожен інструмент стає локальною корекцією в межах свого класу.

| інструмент | клас | що робить |
|---|---|---|
| `eye_vessels` | `l_eye`, `r_eye` | знижує надлишок червоного в білку |
| `teeth` | `mouth` | прибирає жовтизну |
| `mattify` | шкіра | стискає відблиск до навколишнього тону |
| `skin_tone` | шкіра | вирівнює хроматичність |

Спільна форма: `(img, cls, params) -> (result, coverage)`. Кожен віддається
окремим шаром і вимикається окремо (§1).

**Кожен лишається у своєму класі — рівно нуль пікселів поза ним.** Це не
побажання: на вії (§5) і на ланцюжок (§15) ми вже наступали, і обидва рази
через те, що альфа розповзалась ширше за маску. Тут вона множиться на клас
в останню чергу, і тестом перевіряється, що поза класом нуль.

Три рішення, які виглядають дрібними, але визначають, чи буде помітно
ретуш:

**Судини — не відбілювання.** Яскравість білка не чіпається взагалі,
знижується лише надлишок червоного над рештою. Відбілене око видно одразу.

**Зуби не доводяться до нейтралі.** Мета — зменшити жовтизну, а не прибрати
її. Заміряно: в оригіналі синій нижчий за середнє з решти на 0.15, з
дефолтом виходить 0.06, а при силі 0.9 — 0.00, тобто рівно сірі. Саме це
й читається як вставні.

**Матування відносне, не абсолютне.** Відблиск — це локальний викид
яскравості над сусідами, тож і прибирається відносно сусідів. Абсолютний
поріг зачепив би світлу ділянку щоки разом із блиском.

Порядок у конвеєрі: після лікування, до D&B. Номер у назві файлу — це
порядок складання, і D&B отримує номер ПІСЛЯ всіх звичайних шарів
(жорсткий «03» стикався з третім інструментом).

## 18. Інтерфейс: вкладки за етапами, форма з дата-класів

§1.1 описав цільову форму — вкладки за етапами конвеєра. До цієї редакції
UI закривав лише ретуш: проявлення, світлотінь і дрібні інструменти
працювали з CLI й пресетів, але руками в застосунку були недосяжні.
Тепер вкладок одинадцять, і вони покривають усі етапи, що є в коді.

### 18.1 Форма будується зі СХЕМИ, а не пишеться руками

Вкладки «Проявлення», «Світлотінь» і «Інструменти» не містять жодного
поля, набраного в HTML. Сервер віддає `/api/schema` — ту саму схему, яку
читає агент через `--schema`, — а сторінка малює з неї контрол на кожне
поле разом з описом.

Причина не в економії розмітки. Поле в дата-класі і поле в UI — це два
місця, які МУСЯТЬ збігатися, і рано чи пізно розійшлися б: параметр
додано, схема оновилась сама (вона з коду), а вкладка лишилась старою, і
дізналися б ми про це від фотографа. Тепер такого стану не існує:
`DevelopParams` дістав поле — воно є на вкладці, з тим самим описом, що
читає агент.

Наслідок для §1.2, пункт 2: опис поля тепер не лише для агента. Він
стоїть підписом під контролом, тобто вимога «параметр має означати те,
що означає у фотографії» перевіряється очима щодня.

### 18.2 Пресет — це і є стан UI

Ліві повзунки лишились за лікуванням. Усе інше — проявлення, D&B,
інструменти — живе в застосунку **одним словником пресету**, тим самим,
що лягає у файл і що пише агент.

Це не деталь реалізації, а те, заради чого §1.2 писався. Формат один на
три входи: агент пише — UI показує й дає правити — CLI застосовує. Кнопка
«Записати в presets/» віддає файл, який `--preset` бере без переробки, а
клац по пресету в списку накладає його поверх активного, бо накладання —
сенс формату, а не зручність.

Порожнє поле означає «не задано», а не «нуль». Різниця змістова: пресет
частковий, і `contrast: 0` («вирівняй у нуль») — не те саме, що
відсутній ключ («не чіпай»). Тому очищення поля ВИДАЛЯЄ ключ.

### 18.3 Етап рахується від кадру ДО себе

D&B і інструменти запускаються окремими кнопками, поверх уже полікованого
кадру: лікування на 26 Мп коштує хвилини, а посунути силу хочеться
десять разів підряд.

З цього випливає вимога, якої в CLI не було, бо там кожен етап іде рівно
раз: **повторний запуск має давати те, що показує повзунок, а не суму**.
Тому кожен етап тримає кадр, від якого рахувався, і другий прогін
починається з нього. Заміряно: розбіжність між першим і другим прогоном
0.00 кванта.

Звідси ж скидання вниз по ланцюжку. Перелікування робить карти
інструментів і D&B недійсними — вони зняті з іншого кадру, а `layers()`
бере з них базу шару. Лишити їх означало б записати шар, знятий з
неіснуючого стану.

### 18.4 Дві помилки, знайдені при цьому

**Схема оголошувала форму, якої `apply()` не приймала.** Розділи
називалися `tools.teeth`, і обидва очевидні прочитання не спрацьовували:
`tools.teeth:` окремим ключем ішов у «невідомий ключ», а
`tools: {teeth: {...}}` клав словник у `cfg.tools` — інструмент після
цього запускався з ДЕФОЛТАМИ. Друге гірше: пресет виглядав застосованим.
Тепер розділ приймає всі три природні форми (список, словник із
параметрами, `false` для вимкнення), а `merge` іде вглиб рекурсивно —
`tools` вкладений двічі, і зупинка на першому рівні затирала б
налаштування зйомки цілим інструментом заради однієї правки кадру.

**Набір `test_webui` перелічував тести списком імен.** Доданий тест
мовчки не запускався, а набір лишався зеленим, нічого не перевіривши. Це
той самий збій, що вже був у `test_cli` з жорстким списком прапорців.
Порядок там справді значущий (тести ділять один сервер і один відкритий
кадр), але порядок ВИЗНАЧЕННЯ дає його безкоштовно.

## 19. XMP: міст до проявника, який уже є

PLAN.md §1 поділив XMP на три яруси; тут — що з цього зроблено і де
ярус довелося ЗНИЗИТИ проти плану.

### 19.1 Ярус виявився вужчим, ніж планувалось

План відносив увесь кроп до яруса A («геометрія — це арифметика, тут
наближень немає»). На письмі виявилось, що це правда лише при нульовому
куті. В ACR рамка кропа лежить у **вирівняному** кадрі, а `apply_pixels`
ріже ДО повороту (§16, і цей порядок правильний: далі все рахується по
кадру, який людина бачить, зокрема ширина обличчя для `radius_for`).
Отже при `CropAngle ≠ 0` збігу країв немає.

Рішення: рамка без кута — ярус A, рамка з кутом — ярус B з поясненням у
звіті. Назвати її точною було б рівно тією помилкою, від якої застерігає
PLAN.md §5.

**Температура й відтінок опущені до яруса C.** План відносив їх до B
(«перерахунок у user_wb»). Але `user_wb` — це множники СИРИХ каналів, а
температура в ACR задається відносно профілю камери; без матриці з DNG
перерахунок є вгадуванням. `WhiteBalance="As Shot"` натомість лягає в
ярус A точно: це камерний ББ, тобто дефолт libraw.

### 19.2 Звіт — частина результату, а не журнал

Кожен прочитаний параметр потрапляє рівно в один з трьох списків, і всі
три показуються разом: `=` точно, `≈` наближено, `×` не застосовано.
Тест вимагає, щоб жоден ненульовий параметр не пройшов повз звіт.

Два рішення, які виглядають дрібними:

**Параметрична крива звітується гуртом.** Highlights/Shadows/Whites/
Blacks мають спільну причину, і головне в ній не «не вміємо», а те, що
**точкова крива в ACR лежить ПОВЕРХ них**. Взяти лише точкову — значить
застосувати половину тонової правки, зовні не відрізнивши від цілої.

**Температура при `As Shot` не показується серед втраченого.** ACR пише
її завжди; при As Shot це опис того, що зняла камера, а не вказівка.
Фальшива тривога тут дорожча за пропущену: від звіту, у якому половина
рядків не про справу, починають гортати не читаючи.

### 19.3 Порядок накладання

```
XMP  ->  пресет зйомки  ->  пресет кадру  ->  прапорці / повзунки
```

XMP першим, бо це не уточнення, а **база**: те, що фотограф уже вирішив.
Якби він ішов останнім, «застосуй мій стиль до всієї теки» не діяло б
саме на кадрах із сайдкаром.

У пакеті XMP читається ПОКАДРОВО (`batch.process(use_xmp=True)`): у теці
зі зйомки в кожного кадру свій кроп і своя експозиція, і саме вони дають
покадрову різницю, якої пресет на зйомку дати не може.

### 19.4 Наслідок для §6.2, який не можна проґавити

Поріг детекції міряється поверх проявлення. Кадр, проявлений нашим
ярусом B, і кадр з ACR — це різні входи, і пороги з одного на інший не
переносяться. Це те саме, що вже виміряно для декодерів (§4): rawpy
проти ImageIO дає 1478 проти 4613 плям на тому самому порозі.

Тому в досьє для агента (`scripts/brief.py`) блок `camera_raw` іде
поруч із таблицею порогів, і рядки з `×` підписані окремо: кадр цього
НЕ отримав, будувати на них рішення не можна.
